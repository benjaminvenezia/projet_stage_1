                {% if  themeName  not in  themes %}
                    <p>{%trans%}Ce thème n'existe pas.{%endtrans%}</p>
                {% else %}
                    <h1>{{ themeName }}</h1>
                {% endif %}

                <div class="container container__descriptiontheme  container__descriptiontheme--spring2">
                    <div class="row">
                        <div class="col ">
                                <p class="mb-5">{%trans%}Car les saisons se suivent inévitablement, le printemps finit toujours par revenir, et car le terme "évolution" est tissée dans les mailles que définissent la vie,  
                                                je prendrais le temps d'extraire de cette expérience les points positifs qui m'ont fait grandir.{%endtrans%}</p>                             <p>
                                   {% if articles|length > 0 %}
                                    <a href="{{ path('article_show', {theme:'spring2', step: 1}) }}" class="btn btn-success btn-sm">{%trans%}Découvrir{%endtrans%}<i class="fas fa-book-reader"></i></a>
                                {% else %}
                                    <a href="{{ path('article_create') }}" class="btn btn-success btn-sm">{%trans%}créer le premier article.{%endtrans%}<i class="fas fa-feather-alt"></i></a>
                                {% endif %}
                                
                            </p>
                        </div>
                        <div class="col container__img">
                            <img src="{{ asset('/build/img/svg/bourgeon.svg') }}" alt="Un bourgeon.">
                        </div>
                    </div>
                </div>

                
                

                