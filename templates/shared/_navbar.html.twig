<nav class="nav nav__main pt-3">
    <ul>
        <li><a href="{{ path('navigation_homepage') }}">Menu</a></li>
        <li>
            <div class="dropdown">
                <span>Thèmes</span>
                <div class="dropdown-content">
                    {% for t in themes %}
                        <p><a class="dropdown__links" href="{{ path('navigation_theme', {'theme' : t}) }}">{{ t }}</a></p>
                    {% endfor %}
                </div>
            </div>
        </li>
        <li><a href="{{ path('article_create') }}">Créer un article</a></li>
        <li><a href="{{ path('administration_administrate') }}">Panneau d'administrateur</a></li>
    </ul>
    <ul>
        {% if not app.user %}
            <li><a href="{{ path('security_login') }}">Connexion</a></li>
        {% endif %}

        {% if app.user %}
            
            <li><a href="{{ path('security_logout') }}">Déconnexion</a></li>
            <li> Hello {{ app.user.email }}</li>
        {% endif %}

    </ul>
</nav>